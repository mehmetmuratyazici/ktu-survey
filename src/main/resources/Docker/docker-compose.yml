version: '3.1'

services:

  db:
    container_name: postgres-db3
    image: postgres
    restart: always
    ports: 
      - 5432:8087
    environment:
      POSTGRES_PASSWORD: 1

  tomcat-web-server:
    container_name: tomcat-server
    image: tomcat:latest
    ports:
      - 8080:8080